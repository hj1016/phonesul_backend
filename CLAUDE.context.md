# CLAUDE.md — ③ 앱별 컨텍스트: 폰술 실시간 건배방 (서버)

> 3-파일 구조 중 셋째. **서버 레포 전용.** 실시간 건배방 백엔드의 기능·기술 결정을 담는다.
> 다른 아이디어로 갈 땐 이 파일을 새로 쓴다. ①pipeline(공유)·②platform(서버 연동)은 유지.

## 0. 정체성

- 이름: **폰술 건배방 서버** (phonesul-server)
- 정의: 회식 주최자가 방을 만들고 코드를 공유하면, 참여자들이 코드로 입장해 **누군가 "짠"을 누르면 방 전원 화면에 동시에 짠 연출**이 뜨는 실시간 건배 백엔드.
- 위치: 프론트(폰술 미니앱)에 통합되는 기능. 서버는 별도 레포(phonesul-server).
- 톤: 프론트 승계 — 음주 미화·조장 금지, 대체·절제. "함께 짠"은 경쟁이 아니라 동시 축하.

> 기능정의서 v0.2의 "후순위(실시간 동기화 건배)"를 백엔드 추가로 MVP에 편입. → 사양 변경이므로 기능정의서·화면설계서 개정 필요(선우 검수).

## 1. 핵심 기능

| # | 기능 | 동작 |
|---|---|---|
| 1 | 방 생성 | 주최자(방장)가 방 생성 → 서버가 랜덤 코드 발급 |
| 2 | 코드 참여 | 참여자가 코드 입력 → 방 입장(인원 목록 갱신) |
| 3 | 실시간 짠 | 방 안 누구나 "짠" → 서버가 방 전원에게 브로드캐스트 → 모두의 잔에 짠 연출 |
| 4 | 방 상태 동기화 | 입장·퇴장·인원수 실시간 반영 |
| 5 | 방 종료·만료 | 방장 종료 또는 유휴 만료 시 방·상태 폐기 |

> 경쟁·순위·점수 없음(비게임, 프론트 §6 승계). 짠은 동시 축하 연출일 뿐.

## 2. 통신 구조

```
[주최자 미니앱] --(방 생성 요청)--> [FastAPI] --(코드 발급)--> [주최자]
[주최자] --(코드 공유: 앱인토스 share/getTossShareLink 또는 구두)--> [참여자들]
[참여자 미니앱] --(wss 연결 + 코드)--> [FastAPI] --(방 입장)--> [방 멤버십]
[누군가] --(wss "짠" 전송)--> [FastAPI] --(방 전원 broadcast)--> [전원 짠 연출]
```

- **wss 채널 단위 = 방(room).** 코드로 방을 식별, 같은 방 소켓들에 broadcast.
- 코드 공유는 앱인토스 공유(share) 또는 단순 코드 구두 전달 모두 지원(외부 웹 랜딩 아님 → platform 정합).
- 실시간은 우리 서버 내부 wss로만(platform §1). 앱인토스 API 호출 최소화(§2 부담 분리).

## 3. 데이터 구조 (휘발성)

서버 메모리 기준(영속 DB 지양, platform §3).

| 항목 | 설명 |
|---|---|
| Room | { code, hostToken, createdAt, lastActiveAt, members[] } |
| Member | { memberId(서버발급), socketId, joinedAt } — 실명·연락처 저장 안 함 |
| 코드 | 랜덤 6자(영문대문자+숫자, 혼동문자 제외 0/O/1/I). → `// ASSUMPTION:` 자릿수·문자셋 임의값 |
| 만료 | 유휴 N분 후 폐기. → `// ASSUMPTION:` N 임의값 |

> 익명·휘발 원칙(platform §3). getAnonymousKey 등 토스 식별값을 서버에 영속 저장하지 않는다.

## 4. 기술 구현 결정

- **FastAPI + WebSocket(wss).** Python. (VSCode 개발)
- 방·소켓 관리: ConnectionManager 패턴(방별 active 소켓 집합). 표준 FastAPI WebSocket.
- 메시지 포맷: JSON. 타입 필드로 분기(`join`/`leave`/`cheers`/`roster`/`error`).
- 상태 저장: 1차 in-memory(단일 인스턴스). 다중 인스턴스 확장은 후순위(필요 시 Redis pub/sub). → `// ASSUMPTION:` 1차 단일 인스턴스 가정.
- mTLS는 앱인토스 API를 서버에서 호출할 때만(platform §2). 건배방 wss엔 불필요.
- CORS/Origin: 미니앱 WebView Origin 허용 범위 확인. → `// UNVERIFIED:` 토스 WebView Origin 값.

### 4.1 동시성·신뢰성

- 브로드캐스트 원자성: 한 명의 짠이 방 전원에게 1회씩. 중복·유실 최소화.
- 재연결: 소켓 끊김 시 코드로 재입장. 방 상태 복구(roster 재전송).
- 백프레셔: 다발 짠 연타 → 디바운스/쿨다운(서버측). → `// ASSUMPTION:` 쿨다운 값 임의.

## 5. 음주 콘텐츠·정책 대응 (프론트 §5 승계)

- 서버가 생성·전송하는 문구(에러·안내)도 음주 권장 톤 금지. 프론트 문구 가이드(금지→대체) 동일 적용.
- "짠"은 동시 축하. 누가 먼저/많이 같은 경쟁·집계 금지(비게임).
- 술 브랜드 실명 금지(방 이름·메시지 포함).

## 6. 비게임 유지 (프론트 §6 승계)

- 점수·랭킹·리더보드·승패 없음. 짠 횟수 집계해 순위 매기는 것 금지.
- 방은 함께 축하하는 소셜 공간이지 경쟁 공간 아님.

## 7. 검수·비기능

- 실시간 반응: 짠 → 전원 반영 체감 지연 최소(목표 수백 ms). → `// REVIEW(실측):` 대상 환경 측정.
- 안정성: 다수 인원·재연결·연타에도 누수·크래시 없음.
- 보안: 코드 추측 남용 방지(시도 제한·만료, platform §3).
- 개인정보: 익명·휘발(platform §3).

## 8. 검수 마커 집계 (context)

| 항목 | 마커 | 비고 |
|---|---|---|
| 코드 자릿수·문자셋 | `ASSUMPTION` | 임의값, 검수 조정 |
| 방 유휴 만료 N분 | `ASSUMPTION` | 임의값 |
| 짠 쿨다운 값 | `ASSUMPTION` | 임의값 |
| 1차 단일 인스턴스 | `ASSUMPTION` | 확장은 후순위 |
| 토스 WebView Origin | `UNVERIFIED` | CORS 허용 범위 |
| 실시간 반응 지연 | `REVIEW` | 대상 환경 실측 |
| 개인정보 저장 범위 | `REVIEW` | 익명·휘발(platform §3) |
| 로그인 강제 여부 | `REVIEW` | UX 결정(platform §3) |
