# CLAUDE.md — ② 서버 플랫폼 규칙 (앱인토스 서버 연동)

> 3-파일 구조 중 둘째. **서버 레포 전용.** 프론트(미니앱) ②platform과 다른 파일이다.
> 프론트 ②는 미니앱 심사·UX·자산 정책을 담고, 이 파일은 **서버↔앱인토스 연동 규약·서버 운영 제약**을 담는다.
> **기준 시점 2026-06-22.** 앱인토스 규약은 수시 개정되므로 출시 직전 공식 문서 재확인. (출처: developers-apps-in-toss.toss.im)

## 0. 역할 분담

- 이 파일은 **서버가 지켜야 할 정책·연동 규약(what)**만 담는다. FastAPI 사용법·wss 구현법(how)은 Claude Code·공식 문서에 위임.
- 앱인토스 API 사용법(서명·요청 예제)은 공식 문서(API 사용하기)·MCP에서 가져온다.

> 이 프로젝트의 서버는 "건배방 자체(클라이언트↔FastAPI wss)"와 "앱인토스 연동(서버↔앱인토스 API)" 두 통신이 있다. 둘은 규약이 다르다(§1 vs §2).

## 1. 건배방 내부 통신 (클라이언트 ↔ FastAPI) — 우리 서버 자유 영역

- 방 생성·코드 참여·실시간 "짠" 브로드캐스트는 **우리 FastAPI 서버 내부 로직**이다. 앱인토스 API 규약(§2)과 무관.
- **wss 사용 가능** (운영자 확인 2026-06-22). → `// REVIEW(wss: 운영자 채널톡 확인 2026-06-22):` 근거 기록 유지. 정책 개정 가능성 있어 출시 전 재확인.
- 미니앱 WebView에서 우리 서버로 직접 wss 연결. 핵심 기능은 미니앱+우리 서버 안에서 완결(프론트 ②§2 외부링크 제한과 정합 — 외부 웹페이지 랜딩 아님, 데이터 통신임).

> 즉 건배방 실시간 로직은 표준 FastAPI WebSocket으로 설계한다. 제약은 §3(서버 운영) 정도.

## 2. 앱인토스 API 연동 (서버 ↔ 앱인토스) — 규약 필수

앱인토스 기능(로그인 검증·공유 등)을 **서버에서** 호출할 때만 적용. 건배방 wss에는 불필요.

- **mTLS 필수.** 앱인토스 API 호출은 mTLS 기반 server-to-server 통신. 콘솔에서 mTLS 인증서 발급 → 인증서/키를 서버에 등록. → `// REVIEW(mTLS 인증서 발급·등록):` 사람 확인.
- **구조:** 파트너사(우리) 서버 → 앱인토스 서버 → 토스 서버 전달 구조.
- **요청 한도:** 미니앱 기준 분당 최대 3,000 QPM. 실시간 다발 호출 시 한도 고려. → `// ASSUMPTION:` 건배 브로드캐스트가 앱인토스 API를 호출하지 않으면 이 한도와 무관(내부 wss만 사용 설계 권장).
- **응답 규격:** 모든 앱인토스 API는 공통 응답 구조. `resultType`(SUCCESS/FAIL) 먼저 검사 후 분기. FAIL이면 `error.errorCode`·`error.reason` 처리.
- 앱인토스가 가맹점 서버로 보내는 콜백(있다면) 수신 IP 화이트리스트 확인. → `// UNVERIFIED:` 건배방이 콜백을 쓰는지 여부.

> 설계 원칙: **건배방 실시간은 §1(내부 wss)로만 처리하고, 앱인토스 API 호출을 최소화**한다. 그러면 mTLS·QPM 부담을 핵심 경로에서 분리할 수 있다.

## 3. 서버 운영 제약

- **개인정보 최소 수집.** 건배방은 익명 참여 지향. 실명·연락처·토스 식별정보 저장 금지(필요 시 프론트 getAnonymousKey 값만, 서버는 방 단위 임시 식별). → `// REVIEW(개인정보 저장 범위):`.
- **방 데이터 휘발성.** 방·참여자·상태는 메모리 또는 단기 저장(방 종료 시 폐기). 영속 DB에 음주 관련 개인 행동 누적 금지(프론트 비게임·기록 제거 방침과 정합).
- **인증/식별.** 방장·참여자 구분은 서버 발급 토큰/세션으로. 토스 로그인 강제 아님(미니앱은 토스 로그인만 허용이나, 건배방 참여까지 로그인 강제할지는 UX 결정). → `// REVIEW(로그인 강제 여부):`.
- **남용 방지.** 방 생성·코드 추측 무차별 시도(brute force) 대비. 코드 길이·시도 제한·만료. → §context에서 구체화.

## 4. 출시 불가·금지 (프론트 ②§1 승계)

서버도 다음을 유발하면 안 됨:
- 사행성·베팅(누가 먼저 짠 같은 경쟁·순위·보상 금지 — 건배는 동시 축하).
- 현금성 보상·결제 연동(건배방엔 없음).
- 음주 조장·미화로 읽히는 서버 측 문구(푸시·메시지) 생성 금지.

## 5. 검수 마커 집계 (서버)

| 항목 | 마커 | 비고 |
|---|---|---|
| wss 사용 가능 | `REVIEW` | 운영자 확인 2026-06-22, 출시 전 재확인 |
| mTLS 인증서 발급·등록 | `REVIEW` | 앱인토스 API 호출 시 |
| 앱인토스 콜백 사용 여부 | `UNVERIFIED` | 건배방이 콜백 쓰는지 |
| QPM 한도 영향 | `ASSUMPTION` | 내부 wss만 쓰면 무관 |
| 개인정보 저장 범위 | `REVIEW` | 익명·휘발 원칙 |
| 로그인 강제 여부 | `REVIEW` | UX 결정 |
| 방 코드 남용 방지 | `REVIEW` | 길이·시도제한·만료 |
