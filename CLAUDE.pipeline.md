# CLAUDE.md — ① 파이프라인 방법론

> 3-파일 구조 중 첫째. 단계·게이트·검수 규칙을 정의한다. 앱·플랫폼이 바뀌어도 유지한다.
> 건강폰술·앱인토스 종속 내용은 여기에 쓰지 않는다 → ②③로 분리.

## 0. 목적

- 이 파이프라인은 다양한 아이디어를 앱으로 만드는 **재사용 가능한 AI 개발 절차**다. 중심 도구는 Claude Code, 단계 사이에 사람 검수 게이트를 둔다.
- 새 아이디어 적용 시: ①의 단계·게이트·검수 규칙 유지, 앱은 ③ 교체, 플랫폼은 ② 교체.

> 평가 대상은 앱이 아니라 파이프라인의 재사용성. 재사용성 = 같은 출력 재현이 아니라 다양한 입력·플랫폼에 적응.

## 1. 원칙

- **What 고정 / How 위임.** 이 파일은 단계·역할·수용 기준·가드레일(what)을 정한다. 구현 방법(how)은 Claude Code에 위임한다. 단, 단계에 도구를 배치하는 것(stage-tool 매핑)은 what이다.
- **우선순위: (B) 인간 개입 최소화 > (D) 전환 비용 최소화.** 충돌 시 B 우선.
- **도구는 단계가 요구하면 쓰고 아니면 줄인다.** 중심 루프(기획→구현→테스트→검수)는 Claude Code로 통일. Claude Code가 못 하는 일(UX 자산 생성, 실제 실행 환경 검증)에만 별도 도구/사람 추가.

> 도구가 늘수록 전환·일관성 비용이 커져 B·D 둘 다 악화된다.

## 2. 단계 (Stages)

각 단계는 입력 → 출력 → 수용 기준으로 정의. how는 명시 안 함.

| 단계 | 입력 | 출력 | 수용 기준 |
|---|---|---|---|
| S1 기획·스펙 해석 | 아이디어, 플랫폼 공식 문서 | 기능정의서, 화면설계서, 데이터 구조 | 스펙이 문서 근거를 갖고 화면 흐름·상태·전환 확정 |
| S2 구현 | S1 + ②③ | 동작 코드 | 스펙 충족, 금지사항 미위반, 검수 마커 표기 |
| S3 테스트 | S2 코드 | 단위·통합 결과 | 핵심 인터랙션 통과, 대상 실행 환경에서 플랫폼 의존 기능 동작 확인 |
| S4 검수 | S2·S3 | 통과 또는 수정요청 | 검수 마커 전부 사람이 해소, AI 흔적·UX 자연성 통과 |
| S5 배포 | S4 통과물 | 배포본·제출 산출물 | 플랫폼 검수 요건 충족, 되돌리기 어려운 값 사람 확인 |

> 구체 입출력 항목은 ③에서 채운다. 이 표는 골격.

## 3. Stage-Tool 매핑 (기본값)

| 단계 | 도구 |
|---|---|
| S1·S2·S3(코드)·문서 초안 | Claude Code (중심 루프) |
| S3 플랫폼 의존 기능 검증 | 사람 + 대상 실행 환경 |
| UX 자산(필요 시) | 사람 선별 → 생성 AI/공개 라이선스 |
| S4 검수·자산 일관성·라이선스 | 사람 (항상) |
| Git·Notion·WBS | AI 도구 아님, 별도 층 |

> 도구 변경이 필요하면 ① 구조는 바꾸지 말고 변경 사유만 별도 기록.

## 4. Human Review Gate

- 단계 사이에 사람 승인 지점을 둔다. 통과 못 하면 다음 단계 금지.
- 게이트는 "AI가 잘했나"가 아니라 "사람이 책임질 판단이 드러났나"를 본다.

> AI 실수는 팀 책임. 그래서 사람이 볼 지점을 드러내는 게 게이트의 목적.

### 4.1 검수 마커 (세 조건 중 하나일 때만 단다)

| 마커 | 조건 |
|---|---|
| `// UNVERIFIED:` | 공식 문서로 검증 못 한 외부 동작 (SDK·정책 경계·라이브러리) |
| `// ASSUMPTION:` | 문서에 없어 임의로 정한 값·동작 (수치·계수·미명세 엣지케이스) |
| `// REVIEW(사유):` | 되돌리기 어렵거나 사용자에게 노출되는 것 (변경 불가 값·노출 문구·배포 설정) |

- 평범하게 검증된 코드엔 마커 금지. 마커 목록 = "이번 단계에서 팀이 책임질 판단 전부"가 되게 좁게 유지.
- 작업 후 마커를 모아 검수 체크리스트로 출력.

## 5. 작업 전 의도 선언

구현·수정 시작 전 먼저 출력:
- **목표**: 충족할 스펙/요구사항 (출처 문서·조항)
- **접근**: 택한 방법·대안·선택 이유
- **가정**: 문서에 없어 임의로 정한 것 (← 사람 확인 지점)
- **영향 범위**: 건드리는 파일·기능

> AI 실수는 대부분 "명시 안 된 걸 임의로 메운" 지점에서 발생. 가정을 분리해 사람 눈을 끈다.

## 6. 버전 관리·커밋

- 단계별 프롬프트는 `/prompts`에 저장, Git 버전 관리.
- 커밋 접두사: `ai:`(AI 생성, 검수 전) / `review:`(사람 검수·수정 후).
- **`ai:` 커밋만 있는 변경은 머지·배포 금지.**

## 7. 단계 완료 보고

각 단계 종료 시 보고:
- 한 일 요약 (스펙 대비 충족/미충족)
- 검수 필요 지점 (§4.1 마커 집계)
- 사람이 판단할 부분 (AI가 확신 못 하는 것)
- AI 생성 흔적·UX 자연성 위험 지점 (부자연스러운 문구·틀에 박힌 처리)

## 8. 재사용 절차

1. ① 단계·게이트·검수 규칙 유지.
2. ② 플랫폼 규칙 교체.
3. ③ 앱 컨텍스트 새로 작성.
4. §3 매핑은 기본값 유지, 도구 변경 시 사유만 기록.
5. S1부터 게이트 따라 진행.

> 갈아끼우는 작업이 ②③ 교체 + 도구 변경 사유 기록뿐이면 전환 비용 최소화(D) 달성.
