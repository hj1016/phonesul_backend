# CLAUDE.md — 폰술 건배방 서버 AI 개발 파이프라인

> Claude Code가 매 세션 처음 읽는 진입점. **서버 레포(phonesul-server) 전용.**
> 규칙은 아래 3파일에 있고 @import로 불러온다.

@CLAUDE.pipeline.md
@CLAUDE.platform.md
@CLAUDE.context.md

## 파일 역할

| 파일 | 역할 | 교체 시점 |
|---|---|---|
| ① pipeline | 단계·게이트·검수 규칙 | 앱·플랫폼 바뀌어도 유지 (**프론트 레포와 동일 파일 — 복사 공유**) |
| ② platform | 앱인토스 **서버 연동** 규칙(mTLS·QPM·wss·서버운영) | 플랫폼 바뀌면 교체 |
| ③ context | 실시간 건배방 서버 기능·기술 결정 | 아이디어 바뀌면 새로 씀 |

## 우선 원칙

- What 고정(이 문서들) / How 위임(Claude Code·MCP).
- 충돌 시: 플랫폼 정책(②) > 앱 결정(③). 방법론(①)은 별도 층.
- AI 실수는 팀 책임 → 검수 마커(UNVERIFIED/ASSUMPTION/REVIEW)로 사람이 볼 지점 표시.
- 콘셉트 승계: "안 마시는 사람을 위한 앱". 건배는 경쟁 아닌 동시 축하. 음주 미화·권장 금지.

## 이 레포의 위치

- 프론트(폰술 미니앱, 별도 레포)에 통합되는 **실시간 건배방 백엔드**.
- ① pipeline은 프론트 레포의 것과 동일 파일을 복사해 사용한다(파이프라인 재사용성 — 같은 방법론, 다른 ②③).
- 서버는 별도 레포·별도 배포. 프론트와 wss로 통신(②platform §1).
